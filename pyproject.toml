[project]
name = "archastro-github-actions"
version = "0.0.0"
description = "Reusable GitHub Actions for ArchAstro repositories."
requires-python = ">=3.12"
dependencies = [
    "pyyaml==6.0.2",
]

[tool.uv]
package = false
